use flake

# Automatically load the development environment when entering this directory
# Requires direnv to be installed and hooked into your shell

# Optional: Load specific development shell
# use flake .#minimal
# use flake .#testing